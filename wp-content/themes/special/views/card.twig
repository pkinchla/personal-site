<article class="card" id="card-{{post.ID}}">
  {% block content %}
    <h3><a href="{{post.link}}">{{post.title}}</a></h3>
    <time class="sans-regular-italic" datetime="{{post.date|date('c')}}">{{post.date|date('l, F jS, Y')}}</time>
  {% endblock %}
</article>