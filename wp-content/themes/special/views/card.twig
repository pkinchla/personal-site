<article class="card" id="card-{{ post.ID }}">
  {% block content %}
    <a class="h3" href="{{ post.link }}">{{ post.title }}</a>
  <time
    class="sans-regular-italic"
    datetime="{{post.date|date('c')}}"
    >{{post.date|date('l, F jS, Y')}}</time
  >
  {% endblock %}
</article>

